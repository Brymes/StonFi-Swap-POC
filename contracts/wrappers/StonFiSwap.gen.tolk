// Auto-generated wrapper for contract 'StonFiSwap'
//
// This file is automatically generated by 'acton wrapper'
// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "@contracts/stonfi_swap_types"

/// Storage `Storage` is defined in `@contracts/stonfi_swap_types`
struct StonFiSwap {
    address: address
    stateInit: ContractState? = null
}

/// Creates a contract wrapper instance from the storage data
fun StonFiSwap.fromStorage(storage: Storage, toShard: AddressShardingOptions? = null): StonFiSwap {
    val stateInit = ContractState {
        code: build("StonFiSwap"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { stateInit, toShard }.calculateAddress();
    return StonFiSwap { address, stateInit };
}

/// Creates a contract wrapper instance from the address
fun StonFiSwap.fromAddress(address: address): StonFiSwap {
    return StonFiSwap { address };
}

/// Deploys the contract to the blockchain
fun StonFiSwap.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: { stateInit: self.stateInit },
    });
    return net.send(from, genericMsg);
}

fun StonFiSwap.sendUpdateRoutePreset(
    self,
    from: address,
    routePreset: RoutePreset,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: UpdateRoutePreset { routePreset },
    });
    return net.send(from, genericMsg);
}

fun StonFiSwap.sendExecuteSimpleSwapWithReferral(
    self,
    from: address,
    queryId: uint64,
    sourceWalletAddress: address,
    routerWalletAddress: address,
    amount: coins,
    forwardTonAmount: coins,
    swapPlan: SwapPlan,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ExecuteSimpleSwapWithReferral {
            queryId,
            sourceWalletAddress,
            routerWalletAddress,
            amount,
            forwardTonAmount,
            swapPlan: swapPlan.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun StonFiSwap.sendExecuteCrossSwapSameRouter(
    self,
    from: address,
    queryId: uint64,
    sourceWalletAddress: address,
    routerWalletAddress: address,
    amount: coins,
    forwardTonAmount: coins,
    firstPlan: SwapPlan,
    secondPlan: SwapPlan,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ExecuteCrossSwapSameRouter {
            queryId,
            sourceWalletAddress,
            routerWalletAddress,
            amount,
            forwardTonAmount,
            firstPlan: firstPlan.toCell(),
            secondPlan: secondPlan.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun StonFiSwap.sendExecuteCrossSwapDifferentRouters(
    self,
    from: address,
    queryId: uint64,
    sourceWalletAddress: address,
    routerWalletAddress: address,
    amount: coins,
    forwardTonAmount: coins,
    firstPlan: SwapPlan,
    secondPlan: SwapPlan,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ExecuteCrossSwapDifferentRouters {
            queryId,
            sourceWalletAddress,
            routerWalletAddress,
            amount,
            forwardTonAmount,
            firstPlan: firstPlan.toCell(),
            secondPlan: secondPlan.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun StonFiSwap.sendExecuteRefundSwap(
    self,
    from: address,
    queryId: uint64,
    sourceWalletAddress: address,
    routerWalletAddress: address,
    amount: coins,
    forwardTonAmount: coins,
    swapPlan: SwapPlan,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ExecuteRefundSwap {
            queryId,
            sourceWalletAddress,
            routerWalletAddress,
            amount,
            forwardTonAmount,
            swapPlan: swapPlan.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun StonFiSwap.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun StonFiSwap.routePreset(self): RoutePreset? {
    return net.runGetMethod(self.address, "routePreset");
}

fun StonFiSwap.owner(self): address {
    return net.runGetMethod(self.address, "owner");
}
